/// @description Rotate beanstalk anticlockwise
// You can write your code in this editor
//var playerOnBeanstalk = false;
//with (obj_player) {
//	playerOnBeanstalk = place_meeting(x, y, obj_beanstalk);
//}

//// Rotate only if player is on beanstalk or if player is not on beanstalk and is grounded
//if (playerOnBeanstalk || (!playerOnBeanstalk && obj_player.isGrounded)) {
//	var isClockwise = false;
//	rotate_plant(isClockwise);
//}
var isClockwise = false;
rotate_plant(isClockwise);